rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Global Rule: Allow read and write access to all files
    // This unblocks video and image uploads across the entire app for all users and vendors.
    match /{allPaths=**} {
      allow read, write: if true;
    }
    
  }
}
